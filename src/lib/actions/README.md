# actions/

Typed message contracts for communication between content scripts, popup, and service worker. Each action defines a payload type, result type, and a string identifier used for Chrome message routing.
