#!/usr/bin/env bash

function check_exo_tab_folders() {
  local failed=0
  for dir in src/exo-tabs/*/; do
    local name="$(basename "$dir")"
    [[ "$name" == "node_modules" ]] && continue
    # A tab folder needs a popup side (tab.tsx), a page side (page.ts/tsx),
    # or both — page-only modules (richlink, airtable-shelf) are valid.
    if [[ ! -f "${dir}tab.tsx" && ! -f "${dir}page.ts" && ! -f "${dir}page.tsx" ]]; then
      log::err "exo-tab folder '$name' has neither tab.tsx nor page.ts(x)"
      failed=1
    fi
  done
  return "$failed"
}

function main() {
  if ! source "$HOME/.config/bash/plugins/log.bash" &>/dev/null; then
    function log::info { echo "[INFO] [$(date -u +%Y-%m-%dT%H:%M:%S.000Z)] $@" >&2; }
    function log::err { echo "[ERROR] [$(date -u +%Y-%m-%dT%H:%M:%S.000Z)] $@" >&2; }
    function log::dev { :; }
  fi

  log::info "Pre-commit hook"

  log::info "Checking exo-tab folders..."
  if ! check_exo_tab_folders; then
    exit 1
  fi
  log::info "Exo-tab folders OK"

  log::info "Running linter..."
  if ! npx eslint . --ext ts,tsx --max-warnings 0 2>&1; then
    log::err "Lint failed — fix errors and warnings before committing"
    exit 1
  fi
  log::info "Lint passed"

  log::info "Checking formatting..."
  if ! npx prettier --check 'src/**/*.{ts,tsx}' 2>&1; then
    log::err "Formatting issues — run 'npm run format' then re-stage"
    exit 1
  fi
  log::info "Formatting passed"

  log::info "Running type check..."
  if ! npx tsc --noEmit 2>&1; then
    log::err "Type check failed — fix type errors before committing"
    exit 1
  fi
  log::info "Type check passed"

  log::info "Running tests..."
  if ! npx vitest run --reporter=dot 2>&1; then
    log::err "Tests failed — aborting commit"
    exit 1
  fi
  log::info "All checks passed"
}
main "$@"
